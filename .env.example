# Authentication
AUTH_SECRET=""

# Database (Supabase or any PostgreSQL)
DATABASE_URL=""
DIRECT_URL=""

# Socket.io server URL (deploy separately on Railway/Render)
# Leave empty to disable real-time features (app still works via tRPC polling)
NEXT_PUBLIC_SOCKET_URL=""
